{
  "name": "superbrain-graph-kms",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "关联图数据库知识管理系统",
  "workspaces": [
    "api",
    "web"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w api\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w api",
    "seed": "npm run seed -w api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
